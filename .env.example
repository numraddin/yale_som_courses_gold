# Copy to .env and fill in your key. Never commit the real .env.
#
# This file may live here (Lecture 7 folder) or in any parent folder
# (e.g. MGT409/.env) — the backend walks up the tree and loads whichever
# it finds, with the closest one winning.

PORTKEY_API_KEY=your-portkey-api-key-here
